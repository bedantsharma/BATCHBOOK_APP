import { Tabs } from 'expo-router';

// Placeholder — Agent 6 will replace with full tab configuration
export default function OwnerLayout() {
  return <Tabs screenOptions={{ headerShown: false }} />;
}
